<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Accueil{% endblock %} - Le secret de Loli</title>
        
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% set url = app.request.server.get('SYMFONY_APPLICATION_DEFAULT_ROUTE_URL') %}

        {% include 'partials/header.html.twig' %}
            
        <div class="content mt-5">
            {% block body %}{% endblock %}
            <div class="minSite"></div>
        </div>
    </body>
</html>
